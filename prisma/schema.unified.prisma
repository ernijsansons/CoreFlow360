// ============================================================================
// CoreFlow360 - Schema Reference (DEPRECATED)
// ============================================================================
// 
// This file is DEPRECATED and kept only for backward compatibility.
// 
// IMPORTANT: Use schema.prisma as the single source of truth.
// 
// The main schema.prisma contains the complete, validated data model with:
// - All business entities and relationships
// - Proper foreign key constraints
// - Comprehensive indexing strategy
// - Industry-specific models (HVAC, Professional Services, etc.)
// - Advanced features (Consciousness, AI, Analytics)
// 
// To migrate away from this file:
// 1. Update any references to point to schema.prisma
// 2. Regenerate Prisma client: npx prisma generate
// 3. Run database migrations: npx prisma migrate dev
// 
// ============================================================================

// Minimal generator for backward compatibility only
generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "windows", "debian-openssl-3.0.x"]
  output        = "../node_modules/.prisma/client-unified"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================================================
// DEPRECATED: Minimal models for compatibility only
// Use schema.prisma for complete data model
// ============================================================================

model DeprecatedNotice {
  id        String   @id @default(cuid())
  message   String   @default("This schema is deprecated. Use schema.prisma instead.")
  createdAt DateTime @default(now())
  
  @@map("_deprecated_schema_notice")
}